/**
 * Render build entrypoint.
 *
 * This file exists to prevent `ERR_MODULE_NOT_FOUND` when Render (or npm scripts)
 * try to execute `script/build.ts` during the build phase.
 *
 * Update this script with your real build workflow when project files are added.
 */

console.log("[build] build.ts found — running placeholder build step.");
console.log("[build] No project build steps configured yet.");
